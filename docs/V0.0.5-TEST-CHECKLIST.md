# Crafting Core v0.0.5 — Live Test Checklist

## Upgrade / launchers

- [ ] Upgrade from the validated v0.0.4 build and confirm the GM-only Crafting Core launcher still opens.
- [ ] In the Item Directory, confirm the launcher row now contains two controls: `Crafting Core` and `Generate`.
- [ ] Click `Generate` and confirm it opens the Material Generator directly without opening the full GM workbench first.
- [ ] Confirm neither launcher is injected for a non-GM user.

## Preview-first material generation

- [ ] Open `Generate Materials` and generate `Creature Harvest → Undead → Skeletal Undead`.
- [ ] Confirm the generated material rows appear in the Result/Preview area.
- [ ] Before clicking `Create Loot Folder`, confirm **no new world Item folder or Items are created**.
- [ ] Click `Generate Again` and confirm the preview is replaced by a fresh independent roll and still writes nothing to the Item Directory.
- [ ] With a non-empty preview, click `Create Loot Folder`.
- [ ] Confirm the exact preview quantities/names are materialized without a reroll.
- [ ] Confirm one timestamped subfolder is created under `Crafting Core — Generated Loot` only after explicit confirmation.
- [ ] Confirm `Create Loot Folder` is no longer offered for the already materialized preview and the created Folder is displayed.
- [ ] Confirm `Item Directory` opens from the result after folder creation.
- [ ] Generate an empty result and confirm no `Create Loot Folder` action is offered and no folder is created.

## Environment regression

- [ ] Generate `Forest → Flora → Normal` and confirm the preview uses only eligible Material Catalog entries.
- [ ] Confirm `Generate Again` and `Create Loot Folder` behave identically for Environment Gathering.
- [ ] Confirm Scarce / Normal / Rich / Abundant logic remains unchanged from v0.0.4.

## Compact generator UX

- [ ] Open the generator at its default size and confirm the header is compact, with the green d20 icon, title, and short context grouped at the top.
- [ ] Confirm Source + generation parameters fit in a compact options block.
- [ ] Confirm the Result section is visible without resizing the window on a typical notebook-sized viewport.
- [ ] With enough generated material rows to overflow, confirm only the results list scrolls while the action buttons remain reachable.
- [ ] Resize the generator narrower and confirm it remains usable; at very small widths the parameter fields stack vertically.

## Recipe Builder sidebar polish

- [ ] Save a single Recipe draft and confirm the sidebar does not show a pointless scrollbar or clipped entry.
- [ ] Add multiple drafts and confirm the list expands into the remaining sidebar height and scrolls only when genuinely necessary.
- [ ] Confirm Recipe selection highlight remains aligned.

## Existing foundation regression

- [ ] Confirm the `Crafting Core` Compendium folder retains the configured `#8de901` default and Manual sorting behavior.
- [ ] Confirm Materials and Learn Sources packs remain GM-only.
- [ ] Confirm Recipe Builder Save / Publish / Delete Draft remains functional.
- [ ] Confirm a published source can be learned after its Builder draft is deleted.
- [ ] Confirm Character Crafting still consumes inputs and reproduces complete output Item snapshots.
- [ ] Confirm Item Creator-produced consumables still execute their own runtime effects/lifecycle after being crafted.
